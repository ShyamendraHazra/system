#include "include/tests/exp/file_io.h"
// #include <system.h>
// #include <rnd/random.h>
// #include <tests/rnd_string.test.h>
#include <tests/gentestfile.h>
#include <stdio.h>
// #include <tests/exp/file_io.h>
// #include <stdint.h>


int main() {
  char ch;
  // readfile();
  // // writefile_n("files/written.txt", "This is the text I want to write in the written.txt file!");
  // freopen("files/filename.txt", "r", stdin);
  //
  // while(scanf("%c", &ch) != EOF) {
  //         printf("%c", ch);
  // }
  //
  //
  // freopen("/dev/tty", "r", stdin);
  // printf("Current time in second and nanosecond since epoch\n");
  write_file();

  return 0;
}
